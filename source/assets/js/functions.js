window.JOY = window.JOY || {};
(function( window, undefined ){
	// avoid scope look ups
	var document = window.document,
		JOY = window.JOY,
		config = JOY.config;

	if ( false === JOY.mustard ) {
		// Browser failed cuts mustard check in header
		return;
	}

}( window ));